{{template "base" .}}
{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>About Page</h1> 
                <p>This is the about page.</p>
                <p>This cam from the template: {{index .StringMap "test"}}</p>

                <p>{{if ne (index .StringMap "remote_ip") ""}}
                    Your IP is: {{index .StringMap "remote_ip"}}
                    {{else}}
                    I don't know your IP. Visit the <a href="/">Home page</a>
                    {{end}}
                </p>
            </div>
    </div>
{{end}}